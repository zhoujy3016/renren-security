<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.sys.dao.SysDictDao">

	<resultMap id="dictMap" type="hashmap">
		<id column="id" property="id" javaType="Long"/>
		<result column="type" property="type" javaType="String"/>
		<result column="code" property="code" javaType="String"/>
		<result column="value" property="value" javaType="String"/>
	</resultMap>

	<select id="getSysDictEntityGroupByType" resultMap="dictMap">
		select type from sys_dict where del_flag = -1 AND id in
		<foreach item="ids" collection="array" open="(" separator="," close=")">
			#{ids}
		</foreach>
		group by type
	</select>



	<!-- 查询额外的key value放入字典中 -->
	<select id="excuteSelectExtraDictSql" parameterType="string" resultMap="dictMap">
		${value}
	</select>
</mapper>